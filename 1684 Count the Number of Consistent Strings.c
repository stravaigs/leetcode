

int countConsistentStrings(char * allowed, char ** words, int wordsSize){
//allowed - char. words - array. con - 

// use \n to break
int con = 0
for (int i = 0; i < sizeof(allowed) / sizeof(allowed[0]); i++)
{
    for (int j = 0; j < strlen(allowed[i]) - 1; j++)
…        if (count == wordSize)
        {
            con++;
        }
    }
}
return con;
}
